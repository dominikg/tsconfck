/*
multi
line
*/
{
  "compilerOptions": { // trailing single line comment
    "types": /* inline comment */ [
      "//keep this",
      "/*",
      // extra line single comment
      "*/",
      "/* this too */" , //<-dangling
    ] /* dangling->*/, 
    // still dangling ^
  }
}
