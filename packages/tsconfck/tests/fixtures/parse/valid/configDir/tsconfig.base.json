{
  "include": ["${configDir}/**/*"],
  "exclude": ["${configDir}/src/**/*.spec.ts"],
  "compilerOptions": {
    "paths": {
      "$src": ["${configDir}/src/**/*"]
    },
    "outDir": "${configDir}/build"
  }
}